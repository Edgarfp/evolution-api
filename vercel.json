{
  "version": 2,
  "builds": [
    {
      "src": "evolution-api/package.json",
      "use": "@vercel/node",
      "config": {
        "buildCommand": "cd evolution-api && npm install && npm run build"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "evolution-api/dist/main.js"
    }
  ]
}
